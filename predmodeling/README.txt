# the training.csv file is the same one from the original problem set
# the .R code does a quick and dirty dedupping of the data, and builds a logit model and GBM model
# access the objects created in the .R code my loading up logit_gbm.RData, like this:

setwd("/folder/where/file/is/located/")
load("logit_gbm.RData")


